{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"absim","text":"<p>Power-analyze A/B-test criteria on your real production data \u2014 not on toy Gaussians.</p> <p><code>absim</code> is a Monte Carlo simulator for A/B-test statistical criteria. Hand it a NumPy array of historical outcomes from your warehouse \u2014 <code>EmpiricalGenerator</code> bootstrap-resamples your real distribution, injects a calibrated effect, and runs 10 000+ synthetic experiments. The simulator reports \u2014 with a Wilson confidence band \u2014 the false-positive rate under H\u2080 and the power under H\u2081 for every criterion you ask about. The synthetic experiments inherit your data's quirks (zero-inflation, heavy tails, multi-modality, clipping artifacts) \u2014 exactly the things parametric generators won't reproduce.</p> <p>The result: vague intuition (\"CUPED reduces variance\") becomes a number you can show to your team. Not a textbook number from a Gaussian assumption \u2014 your number, on your data.</p>"},{"location":"#real-world-problems-it-solves","title":"Real-world problems it solves","text":"<ul> <li>\"Will CUPED actually be worth the engineering effort on our <code>n</code> and <code>\u03c1</code>?\"</li> <li>\"Is my in-house t-test / bootstrap really calibrated under H\u2080?\"</li> <li>\"For our CTR metric, should we use delta-method, Budylin linearization, or   bootstrap?\"</li> <li>\"My revenue metric has heavy tails \u2014 is t-test still safe, or do we need   bootstrap?\"</li> <li>\"Does post-stratification actually buy us power on our covariate?\"</li> <li>\"I'm prototyping a new criterion \u2014 is its FPR within the binomial CI of \u03b1?\"</li> </ul> <p><code>absim</code> answers each of these by running the experiment thousands of times on synthetic data that looks like yours, then reporting calibration and power side-by-side across every criterion you compare.</p>"},{"location":"#a-typical-session","title":"A typical session","text":"<pre><code>from absim import EffectSize, Simulator\nfrom absim.criteria import CUPED, WelchTTest\nfrom absim.generators import ContinuousGenerator\n\ngen = ContinuousGenerator(n_per_group=1000, sd=1.0, rho=0.6)\n\nfor crit in (WelchTTest(), CUPED()):\n    sim = Simulator(\n        generator=gen,\n        criterion=crit,\n        n_sims=5000,\n        effect=EffectSize(name=\"medium\", value=0.1),\n        seed=0,\n    )\n    rep = sim.run()\n    print(f\"{crit.name:&gt;8s}  power = {rep.power:.3f}  \"\n          f\"({rep.binomial_ci_low:.3f}, {rep.binomial_ci_high:.3f})\")\n</code></pre> <pre><code> welch_t  power = 0.609  (0.595, 0.622)\n   cuped  power = 0.801  (0.789, 0.811)\n</code></pre> <p><code>absim</code> reports the full <code>SimulationReport</code> so you can plot power curves, sweep effect sizes, or write artifacts to parquet.</p>"},{"location":"#what-makes-absim-different","title":"What makes <code>absim</code> different","text":"<ul> <li>Variance-reduction methods are first-class \u2014 <code>CUPED</code>, <code>CUPAC</code>,   <code>PostStratification</code>, <code>PairedStratification</code> ship together under one   <code>Criterion</code> Protocol. Most other open-source statistics libraries cover   none of these.</li> <li>Ratio-metric criteria are first-class \u2014 <code>DeltaMethod</code> and Budylin   <code>Linearization</code> are built in, with generators that produce realistic   numerator\u2013denominator correlation (Poisson sessions \u00d7 per-user rate).</li> <li>The simulator is fast and reproducible \u2014 vectorized hot paths,   <code>joblib</code> parallelism, bit-identical results across <code>parallel=False/True</code>   via <code>SeedSequence.spawn()</code>. 10 000 Welch simulations in ~1.3 s.</li> <li>Calibration is reported with a confidence band \u2014 every rejection rate   comes with a Wilson 95% CI, so you can tell a 0.06 FPR from a real   miscalibration.</li> </ul>"},{"location":"#where-to-next","title":"Where to next","text":"<ul> <li>Power on real data \u2014 feed <code>EmpiricalGenerator</code> your   warehouse pull and run a calibration audit + power analysis on the actual   distribution.</li> <li>Tutorial \u2014 synthesizing data, running the simulator,   reading the report.</li> <li>Criterion reference \u2014 the math behind each   criterion, with assumptions, formulae, and references.</li> <li>Benchmark \u2014 head-to-head comparison across criteria,   metric types, and effect sizes.</li> <li>API reference \u2014 autogenerated from the source.</li> </ul>"},{"location":"benchmark/","title":"Benchmark","text":"<p>Auto-generated by <code>scripts/benchmark.py</code> (1000 sims/cell). Re-run with <code>uv run python scripts/benchmark.py --n-sims 5000</code> for tighter Wilson bands.</p> <p>Each cell is the rejection rate at \u03b1 = 0.05 over 1000 Monte Carlo iterations. The <code>none</code> column is the FPR (should \u2248 0.05); the others are power at the labelled effect sizes. Numbers below come from the on-checkout run; CI re-runs them on every push.</p>"},{"location":"benchmark/#headlines","title":"Headlines","text":"<ul> <li>Continuous + covariate (<code>continuous_*</code>): CUPED and post-stratification   visibly beat Welch at small/medium effects (e.g. n=1000, small effect:   Welch 0.19 \u2192 CUPED 0.26 \u2192 post-strat 0.25).</li> <li>Paired data (<code>paired_*</code>): paired t-test recovers calibration where   Welch becomes overly conservative (FPR drops to \u22480.01 because Welch   ignores the within-pair correlation). Power matches CUPED.</li> <li>Ratio metrics (<code>ratio_*</code>): delta-method and linearization are   empirically indistinguishable (as theory predicts). Both clearly beat a   naive Welch on per-user ratios.</li> <li>Binary: z-proportion and Welch produce essentially identical results   on Bernoulli outcomes \u2014 Welch's overhead disappears at scale.</li> </ul>"},{"location":"benchmark/#binary_n1000","title":"binary_n1000","text":"criterion none small medium large welch 0.0620 0.0680 0.1190 0.3120 z_proportion 0.0620 0.0680 0.1190 0.3120"},{"location":"benchmark/#binary_n20000","title":"binary_n20000","text":"criterion none small medium large welch 0.0390 0.3800 0.8990 1.0000 z_proportion 0.0390 0.3800 0.8990 1.0000"},{"location":"benchmark/#binary_n5000","title":"binary_n5000","text":"criterion none small medium large welch 0.0580 0.1540 0.3840 0.8960 z_proportion 0.0580 0.1540 0.3840 0.8960"},{"location":"benchmark/#continuous_n1000","title":"continuous_n1000","text":"criterion none small medium large bootstrap 0.0420 0.2040 0.5960 0.9970 cuped 0.0480 0.2640 0.8000 1.0000 post_strat 0.0530 0.2510 0.7720 1.0000 welch 0.0440 0.1910 0.6000 0.9980"},{"location":"benchmark/#continuous_n20000","title":"continuous_n20000","text":"criterion none small medium large bootstrap 0.0580 0.9980 1.0000 1.0000 cuped 0.0490 1.0000 1.0000 1.0000 post_strat 0.0470 1.0000 1.0000 1.0000 welch 0.0550 0.9980 1.0000 1.0000"},{"location":"benchmark/#continuous_n5000","title":"continuous_n5000","text":"criterion none small medium large bootstrap 0.0560 0.6950 1.0000 1.0000 cuped 0.0470 0.8620 1.0000 1.0000 post_strat 0.0530 0.8340 1.0000 1.0000 welch 0.0490 0.6940 1.0000 1.0000"},{"location":"benchmark/#paired_n1000","title":"paired_n1000","text":"criterion none small medium large cuped 0.0410 0.2730 0.8000 1.0000 paired 0.0400 0.2760 0.7980 1.0000 welch 0.0110 0.1420 0.6200 1.0000"},{"location":"benchmark/#paired_n20000","title":"paired_n20000","text":"criterion none small medium large cuped 0.0430 1.0000 1.0000 1.0000 paired 0.0440 1.0000 1.0000 1.0000 welch 0.0180 1.0000 1.0000 1.0000"},{"location":"benchmark/#paired_n5000","title":"paired_n5000","text":"criterion none small medium large cuped 0.0550 0.8710 1.0000 1.0000 paired 0.0550 0.8710 1.0000 1.0000 welch 0.0160 0.7410 1.0000 1.0000"},{"location":"benchmark/#ratio_n1000","title":"ratio_n1000","text":"criterion none small medium large delta 0.0520 0.0720 0.1940 0.5780 linearization 0.0520 0.0720 0.1930 0.5780 welch_naive 0.0480 0.0550 0.1730 0.4750"},{"location":"benchmark/#ratio_n20000","title":"ratio_n20000","text":"criterion none small medium large delta 0.0490 0.4870 0.9980 1.0000 linearization 0.0490 0.4870 0.9980 1.0000 welch_naive 0.0530 0.3840 0.9900 1.0000"},{"location":"benchmark/#ratio_n5000","title":"ratio_n5000","text":"criterion none small medium large delta 0.0500 0.1560 0.6970 0.9990 linearization 0.0500 0.1560 0.6970 0.9990 welch_naive 0.0420 0.1210 0.6000 0.9900"},{"location":"changelog/","title":"Changelog","text":"<p>All notable changes to <code>absim</code> will be documented in this file. The format follows Keep a Changelog, and this project adheres to Semantic Versioning.</p>"},{"location":"changelog/#unreleased","title":"[Unreleased]","text":""},{"location":"changelog/#021-2026-05-06","title":"[0.2.1] \u2014 2026-05-06","text":"<p>Polish release after the 0.2.0 real-data pivot. Closes a packaging bug that broke the CLI for <code>pip</code>-installed users, restores <code>mkdocs --strict</code> compatibility, and tightens documentation, tests, and the comparison table.</p>"},{"location":"changelog/#fixed","title":"Fixed","text":"<ul> <li>Packaging: the Hydra <code>conf/</code> tree is now bundled into the wheel via   <code>force-include</code> and located via <code>importlib.resources</code> at runtime. Without   this, <code>absim run experiment=\u2026</code> failed for users installed via PyPI / pip   (the dev-checkout path was the only one that worked).</li> <li><code>mkdocs build --strict</code> now passes \u2014 the bulk-render API pages   (<code>docs/api/criteria.md</code>, <code>docs/api/generators.md</code>) no longer collide with   per-class pages on <code>mkdocs_autorefs</code>. Without this, the gh-pages deploy   workflow would have failed on the next push to <code>main</code>.</li> <li>CONTRIBUTING.md clone command typo (<code>yablochnikov</code> \u2192 <code>yablochnikovds</code>)   fixed; new contributors no longer hit a 404 on the very first command.</li> <li><code>delta.py</code> references corrected \u2014 the H\u00e1jek/\u0160id\u00e1k/Sen Theory of Rank   Tests citation was wrong for the delta method. Replaced with   Casella &amp; Berger \u00a75.5.4 (textbook) and Deng et al. KDD 2018 (applied).</li> <li><code>Simulator</code> is now <code>@dataclass(frozen=True, slots=True)</code> \u2014   consistent with criteria/generators and prevents accidental mid-flight   mutation.</li> <li>Removed the unused <code>wilson_ci</code> import + dead <code>_binomial_ci_99</code> helper from   <code>tests/statistical/test_fpr_power.py</code>.</li> <li>Deduplicated the <code>[0.2.0]</code> CHANGELOG section (previously had stray repeat   blocks from a merge).</li> </ul>"},{"location":"changelog/#changed","title":"Changed","text":"<ul> <li>British\u2192American spelling normalized across narrative text   (<code>linearise \u2192 linearize</code>, <code>vectorised \u2192 vectorized</code>, <code>customise \u2192 customize</code>,   <code>colour \u2192 color</code>, <code>synthesise \u2192 synthesize</code>, <code>visualisation \u2192 visualization</code>,   <code>mis-calibrated \u2192 miscalibrated</code>, etc.). Class names, registry keys, and   YAML keys were already American; the rot was confined to docstring prose.</li> <li>Real-data tutorial Workflow 3 rewritten \u2014 previously titled \"variance   reduction on your CTR metric\"; CUPED applies to any metric with a   pre-experiment covariate (continuous, binary, ratio), not only to ratio   metrics. The example now uses continuous revenue and notes how to extend   to binary/ratio.</li> <li>Comparison table in README softened to honestly reflect   <code>cluster_experiments</code> capabilities: it has CUPED but not CUPAC, and its   bootstrap is for clustered designs, not real-data injection. CUPAC, BCa   bootstrap, ratio-metric linearization, and the calibration-audit harness   are the differentiators we keep highlighting.</li> <li><code>scripts/benchmark.py</code> default <code>--n-sims=1000</code> matches the shipped   <code>docs/benchmark.md</code> numbers; the help text spells out the trade-off   (\"raise to 5000 for tighter Wilson bands at 5x the runtime\").</li> <li><code>pre-commit</code> hooks bumped (<code>ruff</code> 0.7.4, <code>mypy</code> 1.13.0) to track CI.</li> </ul>"},{"location":"changelog/#added","title":"Added","text":"<ul> <li>Test coverage: parametrized <code>BinaryGenerator</code> calibration across   <code>p \u2208 {0.1, 0.3, 0.5, 0.8}</code> plus a saturation test at   unattainable <code>(p=0.05, rho=0.95)</code>. Direct unit tests for   <code>_bca_two_sided_pvalue</code> (centred \u2192 p\u22481; far-tail \u2192 p&lt;1e-3; rejection   matches <code>p_value &lt; alpha</code>). A regression test that pooled-OOF CUPAC is   unbiased for the ATE under H\u2081.</li> </ul>"},{"location":"changelog/#020-2026-05-06","title":"[0.2.0] \u2014 2026-05-06","text":"<p>Real-data simulation is the headline feature of this release: the simulator now drives off your actual production distribution, not just three parametric stand-ins.</p>"},{"location":"changelog/#added_1","title":"Added","text":"<ul> <li><code>absim.generators.EmpiricalGenerator</code> \u2014 bootstrap-resamples from real   observed data (NumPy arrays / pandas columns from your warehouse) and   injects calibrated effects into the treatment arm. Auto-detects   continuous / binary / ratio metric types and supports paired covariates,   strata, and numerator/denominator pairs. Closes the \"but I can't run   absim on my real data\" objection: the simulator now drives off your   actual distribution.</li> <li>New page Power on real data walking through three real-world   workflows: power analysis on warehouse revenue, calibration audit on   in-house statistical code, and CTR-with-CUPED comparison.</li> <li><code>absim._stats.make_result(...)</code> and <code>absim._stats.degenerate_result(...)</code>   helpers and a <code>absim._stats.two_sided_t_pvalue(...)</code> p-value helper that   absorb the <code>TestResult</code> boilerplate from every criterion.</li> <li><code>absim.generators.base.make_strata(...)</code> shared helper used by all   parametric generators for equal-frequency stratum binning.</li> </ul>"},{"location":"changelog/#changed_1","title":"Changed","text":"<ul> <li><code>BinaryGenerator</code> now numerically calibrates the logistic slope   <code>\u03b2</code> (via Brent's method) so the realised <code>corr(Y, X)</code> actually   matches the requested <code>rho</code> to \u2248 1e-3, instead of the previous loose   heuristic <code>\u03b2 = 2\u00b7rho</code> which produced <code>corr \u2248 rho/2</code> for <code>p = 0.1</code>.   Saturates gracefully when the request exceeds what a logistic link can   produce at the given baseline <code>p</code>.</li> <li><code>absim._stats.welch_ttest</code> now returns the Welch\u2013Satterthwaite degrees of   freedom as a fifth tuple element so callers stop recomputing it.</li> <li><code>Simulator.run()</code> now passes <code>batch_size=\"auto\"</code> to <code>joblib.Parallel</code>,   amortizing per-task IPC overhead on fast criteria.</li> </ul>"},{"location":"changelog/#fixed_1","title":"Fixed","text":"<ul> <li><code>Bootstrap(method=\"bca\")</code> previously reported a percentile-style p-value   alongside a BCa CI; rejection now matches the BCa CI exactly via a   BCa-adjusted achieved-significance level.</li> <li><code>PostStratification</code> no longer drops small-<code>n</code> strata silently from the   effect estimate; it falls back to the within-stratum pooled variance   (Miratrix\u2013Sekhon\u2013Yu 2013, \u00a73) for the variance contribution.</li> <li>The <code>__init__.py</code> doctest example now uses the real public API   (<code>EffectSize</code>, <code>Simulator.seed</code>) instead of nonexistent constructor   kwargs.</li> <li><code>CUPAC._oof_predict_pooled</code> silences the actual <code>RuntimeWarning</code> numpy   emits for degenerate LOO folds (the previous narrowing to   <code>ConvergenceWarning</code> was incorrect \u2014 <code>RidgeCV</code> is closed-form and never   emits <code>ConvergenceWarning</code>).</li> <li>Removed the unused <code>absim._stats.make_rng</code> helper.</li> </ul>"},{"location":"changelog/#documentation","title":"Documentation","text":"<ul> <li>README, <code>docs/index.md</code>, package docstring and <code>ARCHITECTURE.md</code>   rewritten around concrete real-data use-cases (\"power-analyze a real   metric from our warehouse\", \"is my in-house t-test calibrated?\",   \"delta-method or linearization for our CTR?\", \"is t-test safe on   heavy-tailed revenue?\") rather than abstract feature lists.</li> </ul>"},{"location":"changelog/#010-2026-05-05","title":"[0.1.0] \u2014 2026-05-05","text":"<p>Initial release.</p>"},{"location":"changelog/#added_2","title":"Added","text":"<ul> <li><code>absim.Simulator</code> \u2014 Monte Carlo engine with <code>joblib</code>-based parallelism   and reproducible per-iteration RNGs via <code>SeedSequence.spawn()</code>.</li> <li>Criteria:</li> <li><code>WelchTTest</code> \u2014 two-sample t-test with unequal variances.</li> <li><code>ZTestProportions</code> \u2014 z-test for difference of proportions.</li> <li><code>CUPED</code> \u2014 pre-experiment-covariate variance reduction.</li> <li><code>CUPAC</code> \u2014 out-of-fold pooled-model variant of CUPED.</li> <li><code>PostStratification</code> \u2014 closed-form stratified mean-difference test.</li> <li><code>PairedStratification</code> \u2014 paired t-test for genuinely paired samples     (rank-matching fallback for unpaired data).</li> <li><code>Bootstrap</code> \u2014 non-parametric resampling with percentile and BCa     intervals (jackknife acceleration).</li> <li><code>DeltaMethod</code> \u2014 Taylor-linearized variance for ratio metrics.</li> <li><code>Linearization</code> \u2014 Budylin per-unit linearization for ratio metrics.</li> <li>Generators:</li> <li><code>ContinuousGenerator</code> \u2014 Gaussian / lognormal / mixture, optional pre-     experiment covariate, optional paired sampling.</li> <li><code>BinaryGenerator</code> \u2014 Bernoulli outcomes with logistic-link covariate.</li> <li><code>RatioGenerator</code> \u2014 Poisson sessions \u00d7 per-user rate with realistic     numerator\u2013denominator correlation.</li> <li>Reports \u2014 <code>SimulationReport</code> with Wilson 95% CI on the rejection rate;   parquet + CSV IO; <code>plot_fpr_bar</code> and <code>plot_power_curve</code>.</li> <li>CLI \u2014 <code>absim run</code>, <code>absim list-criteria</code>, <code>absim list-experiments</code>.</li> <li>Hydra structured configs \u2014 <code>conf/{config,data,criterion,experiment}/*.yaml</code>.</li> <li>Pre-commit, ruff, mypy <code>--strict</code>, pytest with statistical tests.</li> <li>mkdocs + mkdocs-material + mkdocstrings documentation site.</li> <li>GitHub Actions CI matrix on Python 3.10 / 3.11 / 3.12.</li> </ul>"},{"location":"contributing/","title":"Contributing to absim","text":""},{"location":"contributing/#dev-environment","title":"Dev environment","text":"<pre><code>git clone https://github.com/yablochnikovds/ab-simulator\ncd ab-simulator\nuv sync                       # installs all dev + docs deps\nuv run pre-commit install     # set up the local hooks\n</code></pre>"},{"location":"contributing/#running-checks","title":"Running checks","text":"<pre><code>uv run ruff check src tests\nuv run ruff format --check src tests\nuv run mypy src\nuv run pytest                                      # unit + property\nuv run pytest -m statistical                       # FPR / power validation (slower)\nuv run pytest --cov=src/absim --cov-fail-under=85  # full coverage gate\nuv run mkdocs build --strict                       # docs build\n</code></pre>"},{"location":"contributing/#adding-a-criterion","title":"Adding a criterion","text":"<p>A criterion is a <code>@dataclass(frozen=True, slots=True)</code> exposing a <code>test</code> method that returns an <code>absim.TestResult</code>. The whole change is a single new file:</p> <pre><code># src/absim/criteria/my_method.py\nfrom __future__ import annotations\nfrom dataclasses import dataclass\nfrom typing import Any\n\nimport numpy as np\n\nfrom absim.criteria.base import register\nfrom absim.types import TestResult\n\n\n@register(\"my_method\")\n@dataclass(frozen=True, slots=True)\nclass MyMethod:\n    \"\"\"One-line summary; full reference in docs/criteria/my_method.md.\"\"\"\n\n    alpha: float = 0.05\n    name: str = \"my_method\"\n\n    def test(self, treatment, control, **kwargs: Any) -&gt; TestResult:\n        # ... your math here ...\n        return TestResult(\n            p_value=p, statistic=t, effect=delta,\n            std_error=se, ci_low=lo, ci_high=hi,\n            rejected=p &lt; self.alpha,\n        )\n</code></pre> <p>Then:</p> <ol> <li>Re-export it from <code>src/absim/criteria/__init__.py</code>.</li> <li>Add a <code>conf/criterion/my_method.yaml</code> with the <code>_target_:</code> for Hydra.</li> <li>Add a unit test (textbook closed-form), a property test (a stat    invariant), and \u2014 if the method is a real production candidate \u2014 a    statistical FPR test in <code>tests/statistical/</code>.</li> <li>Document the formula and assumptions in <code>docs/criteria/my_method.md</code>.</li> </ol>"},{"location":"contributing/#adding-a-generator","title":"Adding a generator","text":"<p>Same pattern in <code>src/absim/generators/</code>. Implement <code>sample(rng, mean_shift) -&gt; Sample</code>. Emit auxiliary arrays under the standard keys (<code>covariate_*</code>, <code>strata_*</code>, <code>numerator_*</code>, <code>denominator_*</code>, <code>features_*</code>) so existing criteria can consume them.</p>"},{"location":"contributing/#commit-style","title":"Commit style","text":"<p>Conventional Commits. Keep messages short, imperative, \u226472 char subject. Examples:</p> <pre><code>feat(criteria): add Mann\u2013Whitney U test\nfix(simulator): correct seed split when n_jobs=1\ndocs(criteria): clarify CUPED variance-reduction formula\n</code></pre>"},{"location":"contributing/#releasing","title":"Releasing","text":"<p>We use a single source of truth in <code>src/absim/_version.py</code>. Bump it, update <code>CHANGELOG.md</code>, tag, push.</p>"},{"location":"real_data/","title":"Power analysis on your real data","text":"<p>The textbook objection to a Monte Carlo simulator is \"but it generates fake data \u2014 my real metric doesn't look like a Gaussian\". <code>EmpiricalGenerator</code> is the answer: it bootstrap-resamples from historical observations you hand it and injects a calibrated effect into the treatment arm. The synthetic experiments inherit your data's quirks \u2014 zero-inflation, heavy tails, multimodality, clipping artifacts \u2014 that the parametric generators won't reproduce.</p> <p>This page walks through three real-world workflows.</p>"},{"location":"real_data/#workflow-1-power-for-a-continuous-metric-revenue-time-on-page","title":"Workflow 1 \u2014 power for a continuous metric (revenue, time-on-page)","text":"<p>You're about to run an A/B test on per-user revenue. You pulled a representative month from the warehouse. You want to know: at the lift you hope for, how often will the test reject H\u2080?</p> <pre><code>import numpy as np\nimport pandas as pd\nfrom absim import EffectSize, Simulator\nfrom absim.criteria import Bootstrap, WelchTTest\nfrom absim.generators import EmpiricalGenerator\n\n# Replace with your real warehouse pull.\ndf = pd.read_parquet(\"revenue_last_month.parquet\")\nrevenue = df[\"revenue\"].to_numpy(dtype=float)        # zero-inflated, heavy tail\nprint(f\"n={revenue.size}, mean={revenue.mean():.2f}, \"\n      f\"median={np.median(revenue):.2f}, max={revenue.max():.0f}\")\n\ngen = EmpiricalGenerator(outcomes=revenue, n_per_group=10_000, relative=True)\n\nfor crit in (WelchTTest(), Bootstrap(method=\"bca\", n_resamples=1000)):\n    for lift in (0.0, 0.02, 0.05):\n        sim = Simulator(gen, crit, n_sims=2000,\n                        effect=EffectSize(f\"+{lift:.0%}\", lift), seed=0)\n        r = sim.run()\n        label = \"FPR\" if lift == 0.0 else \"Power\"\n        print(f\"  {crit.name:&gt;10s}  lift={lift:+.0%}  {label}={r.rejection_rate:.3f}\")\n</code></pre> <p>Read the FPR column to confirm calibration; read the Power columns at the lift you actually expect to ship.</p> <p>Why bootstrap here? With heavy-tailed revenue, the <code>WelchTTest</code> can over-reject (FPR drifts above 0.05) even at moderate sample sizes. <code>Bootstrap(method=\"bca\")</code> stays calibrated.</p>"},{"location":"real_data/#workflow-2-sanity-check-your-in-house-statistical-code","title":"Workflow 2 \u2014 sanity-check your in-house statistical code","text":"<p>Your prod platform has its own <code>experiments.welch_test()</code> (or its own bootstrap, or its own CUPED implementation). Bugs in here are catastrophic because they show up across hundreds of experiments. You want a calibration audit.</p> <pre><code>from dataclasses import dataclass\nfrom absim import Simulator\nfrom absim.criteria.base import register\nfrom absim.types import TestResult\nfrom absim.generators import EmpiricalGenerator\n\n# Your in-house module:\nimport experiments_platform as platform\n\n\n@register(\"inhouse\")\n@dataclass(frozen=True, slots=True)\nclass InHouseTest:\n    alpha: float = 0.05\n    name: str = \"inhouse\"\n\n    def test(self, treatment, control, **aux) -&gt; TestResult:\n        result = platform.welch_test(treatment, control, alpha=self.alpha)\n        return TestResult(\n            p_value=result.p_value,\n            statistic=result.statistic,\n            effect=result.point_estimate,\n            std_error=result.std_error,\n            ci_low=result.ci[0],\n            ci_high=result.ci[1],\n            rejected=result.p_value &lt; self.alpha,\n        )\n\n\n# Use real data so the audit is realistic, not toy.\ngen = EmpiricalGenerator(outcomes=revenue, n_per_group=5000)\nreport = Simulator(gen, InHouseTest(), n_sims=10_000, seed=0).run()\n\nprint(f\"In-house FPR: {report.fpr:.4f} \"\n      f\"(95% Wilson CI: [{report.binomial_ci_low:.4f}, \"\n      f\"{report.binomial_ci_high:.4f}])\")\n# If 0.05 \u2209 CI, your in-house code is miscalibrated and you have a bug.\n</code></pre> <p>If the Wilson CI brackets \u03b1 (0.05), the in-house code is calibrated. If not, the simulation just caught a real production bug.</p>"},{"location":"real_data/#workflow-3-variance-reduction-with-cuped-on-any-metric","title":"Workflow 3 \u2014 variance reduction with CUPED on any metric","text":"<p>CUPED reduces variance whenever you have a per-unit pre-experiment covariate that correlates with the outcome \u2014 any outcome. This works equally well on continuous metrics (revenue, watch-time), binary metrics (conversion), and ratio metrics (CTR per session). The variance reduction approaches <code>1 \u2212 \u03c1\u00b2</code> where <code>\u03c1 = Corr(outcome, covariate)</code>.</p> <p>The example below uses continuous revenue with a pre-period activity covariate; for binary or ratio metrics swap in the appropriate columns.</p> <pre><code>from absim.criteria import CUPED, WelchTTest\nfrom absim.generators import EmpiricalGenerator\n\n# Real warehouse pull: per-user revenue last month, paired with sessions\n# the month before (the canonical CUPED setup from Deng et al. 2013).\ndf = pd.read_parquet(\"user_metrics_last_month.parquet\")\n\ngen = EmpiricalGenerator(\n    outcomes=df[\"revenue\"].to_numpy(dtype=float),\n    covariate=df[\"sessions_prior_month\"].to_numpy(dtype=float),\n    n_per_group=5000,\n    relative=True,\n)\n\nfor crit in (WelchTTest(), CUPED()):\n    sim = Simulator(gen, crit, n_sims=5000,\n                    effect=EffectSize(\"+5%\", 0.05), seed=0)\n    r = sim.run()\n    print(f\"{crit.name:&gt;8s}  power = {r.power:.3f}\")\n</code></pre> <p>If the CUPED line beats Welch by a meaningful margin (e.g. +20 percentage points of power), the pre-period covariate is informative enough to be worth the engineering work. For binary outcomes (conversion), point the <code>outcomes=</code> column at the 0/1 array and CUPED will run on the linear adjustment of the conversion indicator. For ratio metrics, run <code>Linearization</code> first (or in parallel) \u2014 it usually outperforms CUPED-on-the-realised-ratio on heavily skewed denominators.</p>"},{"location":"real_data/#when-empiricalgenerator-is-the-right-choice-and-when-it-isnt","title":"When <code>EmpiricalGenerator</code> is the right choice \u2014 and when it isn't","text":"<p>\u2705 Use it when you have an actual sample of historical outcomes that captures the distributional structure of your future experiments. The bootstrap respects whatever weirdness is in the sample (point masses at 0, heavy tails, gaps, modes).</p> <p>\u26a0\ufe0f The bootstrap cannot extrapolate: if your historical sample doesn't contain a regime your live experiment will see (e.g. a Black Friday spike), the simulation will under-represent it. Pull a sample that covers the regime you'll experiment under.</p> <p>\u26a0\ufe0f For very small historical samples (&lt; ~500 units), the bootstrap is high-variance. Either pull more data or fall back to a parametric <code>ContinuousGenerator</code> / <code>BinaryGenerator</code> / <code>RatioGenerator</code> with parameters estimated from the small sample.</p> <p>\u274c Don't use it for forward-looking sample-size formulas. If you just need \"how many users for 80% power at MDE 5%\", <code>statsmodels.stats.power</code> or G*Power is the right tool \u2014 they're a closed-form calculation, not a Monte Carlo run.</p>"},{"location":"real_data/#reproducibility","title":"Reproducibility","text":"<p><code>EmpiricalGenerator</code> is bit-identical between <code>Simulator(parallel=False)</code> and <code>parallel=True</code> runs given the same seed. The bootstrap indices are drawn from the per-iteration RNG produced by <code>SeedSequence.spawn()</code>, so re-running the same simulation with the same seed always returns the same report \u2014 important when these reports become artefacts attached to a PR or methodology RFC.</p>"},{"location":"tutorial/","title":"Tutorial: from synthetic data to a calibration report","text":"<p>This walkthrough uses Welch's t-test on a Gaussian outcome to demonstrate the full <code>absim</code> workflow. Once you've finished it, swapping in any other criterion or generator is a one-line change.</p>"},{"location":"tutorial/#1-synthesize-data","title":"1. Synthesize data","text":"<p>Pick a generator. The continuous generator emits a Gaussian outcome plus a pre-experiment covariate (so CUPED has something to work with) and strata indicators (for <code>PostStratification</code>).</p> <pre><code>from absim.generators import ContinuousGenerator\n\ngen = ContinuousGenerator(\n    n_per_group=1000,    # users per arm\n    mean=0.0,            # baseline mean\n    sd=1.0,              # outcome noise\n    rho=0.6,             # corr(Y, covariate)\n    n_strata=4,          # equal-probability strata to emit\n)\n</code></pre> <p>Drawing one sample by hand:</p> <pre><code>import numpy as np\n\nrng = np.random.default_rng(0)\nsample = gen.sample(rng, mean_shift=0.1)  # +0.1 lift on treatment\nsample.treatment.shape, sample.aux.keys()\n# ((1000,), dict_keys(['covariate_treatment', 'covariate_control', ...]))\n</code></pre>"},{"location":"tutorial/#2-pick-a-criterion","title":"2. Pick a criterion","text":"<p>Every criterion is a small dataclass:</p> <pre><code>from absim.criteria import CUPED, WelchTTest\n\nwelch = WelchTTest(alpha=0.05)\ncuped = CUPED(alpha=0.05)\n</code></pre> <p>Single-shot test on <code>sample</code>:</p> <pre><code>result = welch.test(sample.treatment, sample.control, **sample.aux)\nresult.p_value, result.effect, result.ci_low, result.ci_high\n</code></pre>"},{"location":"tutorial/#3-drive-a-monte-carlo-simulation","title":"3. Drive a Monte Carlo simulation","text":"<pre><code>from absim import EffectSize, Simulator\n\nsim_h0 = Simulator(\n    generator=gen,\n    criterion=welch,\n    n_sims=5000,\n    alpha=0.05,\n    effect=EffectSize(name=\"none\", value=0.0),  # H0 \u2192 FPR\n    seed=0,\n)\nreport_h0 = sim_h0.run()                 # parallel by default\nprint(report_h0.fpr, report_h0.binomial_ci_low, report_h0.binomial_ci_high)\n</code></pre> <p>For power, swap the effect size:</p> <pre><code>sim_h1 = Simulator(\n    generator=gen,\n    criterion=welch,\n    n_sims=5000,\n    alpha=0.05,\n    effect=EffectSize(name=\"medium\", value=0.1),\n    seed=0,\n)\nprint(sim_h1.run().power)\n</code></pre>"},{"location":"tutorial/#4-compare-criteria-plot-the-result","title":"4. Compare criteria, plot the result","text":"<pre><code>from absim.reports import plot_power_curve\n\nreports = []\nfor crit in (welch, cuped):\n    for value in (0.0, 0.05, 0.1, 0.2):\n        sim = Simulator(\n            generator=gen, criterion=crit, n_sims=2000, alpha=0.05,\n            effect=EffectSize(name=str(value), value=value), seed=0,\n        )\n        reports.append(sim.run())\n\nfig = plot_power_curve(reports)\nfig.savefig(\"power.png\", dpi=120)\n</code></pre>"},{"location":"tutorial/#5-or-use-the-cli","title":"5. Or use the CLI","text":"<p>Everything above is wired to a Hydra config \u2014 running an experiment is a single command:</p> <pre><code>absim run experiment=continuous_welch_vs_cuped simulator.n_sims=10000\n</code></pre> <p>It writes <code>outputs/.../reports.parquet</code>, <code>reports.csv</code>, <code>fpr.png</code>, and <code>power.png</code>. Add <code>--help</code> after <code>run</code> to see all override flags.</p>"},{"location":"api/","title":"API reference","text":"<p>The full public surface, autogenerated by <code>mkdocstrings</code>.</p> <ul> <li>Simulator + types</li> <li>Criteria</li> <li>Generators</li> <li>Types</li> </ul>"},{"location":"api/criteria/","title":"<code>absim.criteria</code>","text":"<p>The criterion package exposes a single <code>Criterion</code> Protocol plus a registry helper API. Each shipped criterion has its own page under Criterion reference, which doubles as the API reference for that class.</p>"},{"location":"api/criteria/#protocol-registry","title":"Protocol &amp; registry","text":"<p>Criterion protocol and registry.</p> <p>A criterion is anything that can take a sample and return a :class:<code>TestResult</code>. The :data:<code>REGISTRY</code> lookup keeps the simulator + CLI decoupled from concrete implementations, so adding a new criterion is a single-file change (the new module just calls :func:<code>register</code>).</p>"},{"location":"api/criteria/#absim.criteria.base.Criterion","title":"Criterion","text":"<p>               Bases: <code>Protocol</code></p> <p>Protocol every statistical criterion must implement.</p> <p>Concrete criteria are typically dataclasses configured with their hyperparameters (e.g. <code>alpha</code>, <code>B</code> for bootstrap), and the actual work is done in :meth:<code>test</code>.</p>"},{"location":"api/criteria/#absim.criteria.base.Criterion.test","title":"test","text":"<pre><code>test(\n    treatment: FloatArray,\n    control: FloatArray,\n    **kwargs: Any,\n) -&gt; TestResult\n</code></pre> <p>Run the test and return its :class:<code>TestResult</code>.</p> <p><code>**kwargs</code> lets callers pass auxiliary data needed by some criteria (covariates for CUPED, denominators for ratio metrics, strata, ...). Criteria must ignore unknown keys.</p>"},{"location":"api/criteria/#absim.criteria.base.register","title":"register","text":"<pre><code>register(name: str) -&gt; Callable[[_C], _C]\n</code></pre> <p>Class decorator: register a criterion class under <code>name</code>.</p> <p>Used as <code>@register(\"welch_t\")</code> above the class definition. Decoupling the registry key from a class attribute lets concrete criteria use <code>slots=True</code> (which would otherwise strip defaults from the class dict).</p>"},{"location":"api/criteria/#absim.criteria.base.get","title":"get","text":"<pre><code>get(name: str) -&gt; type[Any]\n</code></pre> <p>Return the criterion class registered under <code>name</code>.</p>"},{"location":"api/criteria/#absim.criteria.base.available","title":"available","text":"<pre><code>available() -&gt; list[str]\n</code></pre> <p>Return the sorted list of registered criterion names.</p>"},{"location":"api/criteria/#concrete-criteria","title":"Concrete criteria","text":"Class Registry key Page <code>WelchTTest</code> <code>welch_t</code> Welch's t-test <code>ZTestProportions</code> <code>z_proportion</code> z-test for proportions <code>CUPED</code> <code>cuped</code> CUPED <code>CUPAC</code> <code>cupac</code> CUPAC <code>Bootstrap</code> <code>bootstrap</code> Bootstrap <code>DeltaMethod</code> <code>delta_method</code> Delta-method <code>Linearization</code> <code>linearization</code> Linearization <code>PostStratification</code> <code>post_stratification</code> Post-stratification <code>PairedStratification</code> <code>paired_stratification</code> Paired stratification"},{"location":"api/generators/","title":"<code>absim.generators</code>","text":"<p>The generator package exposes a <code>Generator</code> Protocol, a <code>Sample</code> dataclass, and four concrete generators: one bootstrap-from-real-data (<code>EmpiricalGenerator</code>) and three parametric stand-ins for when historical data isn't available.</p>"},{"location":"api/generators/#protocol-helpers","title":"Protocol &amp; helpers","text":"<p>Generator protocol and shared types.</p> <p>A generator produces a :class:<code>Sample</code>: per-call independent treatment and control arrays plus optional auxiliary structures (covariates for CUPED, strata for post-stratification, paired numerator/denominator arrays for ratio metrics).</p>"},{"location":"api/generators/#absim.generators.base.Generator","title":"Generator","text":"<p>               Bases: <code>Protocol</code></p> <p>Protocol for synthetic data generators.</p>"},{"location":"api/generators/#absim.generators.base.Generator.sample","title":"sample","text":"<pre><code>sample(rng: Generator, mean_shift: float) -&gt; Sample\n</code></pre> <p>Draw one sample with the requested treatment-effect shift.</p> <p><code>mean_shift</code> is interpreted on the metric's natural scale \u2014 the simulator passes <code>0</code> for FPR runs and <code>effect.value</code> otherwise. Generators are expected to be deterministic given the supplied :class:<code>numpy.random.Generator</code>.</p>"},{"location":"api/generators/#absim.generators.base.Sample","title":"Sample  <code>dataclass</code>","text":"<p>A single Monte Carlo sample for one (data, criterion) iteration.</p> <p>Attributes:</p> Name Type Description <code>treatment, control</code> <p>Per-unit outcomes :math:<code>Y_i</code>. For ratio metrics this is the realised :math:<code>N_i / D_i</code> (criteria that ignore it can simply leave it).</p> <code>aux</code> <code>dict[str, Any]</code> <p>Free-form auxiliary arrays \u2014 generators that produce covariates, strata, numerator/denominator pairs, or features for CUPAC place them here using the same kwarg names the corresponding criteria consume (e.g. <code>\"covariate_treatment\"</code>, <code>\"strata_control\"</code>, <code>\"numerator_treatment\"</code>).</p>"},{"location":"api/generators/#absim.generators.base.make_strata","title":"make_strata","text":"<pre><code>make_strata(\n    values_t: FloatArray,\n    values_c: FloatArray,\n    n_strata: int,\n) -&gt; tuple[IntArray, IntArray]\n</code></pre> <p>Bin both arms into <code>n_strata</code> equal-frequency buckets using pooled quantiles.</p> <p>Returns integer stratum labels in <code>[0, n_strata)</code> for each arm. When <code>n_strata &lt;= 1</code> returns all-zero arrays \u2014 the canonical \"no stratification\" fallback that keeps downstream code uniform.</p>"},{"location":"api/generators/#bootstrap-from-real-data","title":"Bootstrap-from-real-data","text":""},{"location":"api/generators/#absim.generators.empirical.EmpiricalGenerator","title":"absim.generators.empirical.EmpiricalGenerator  <code>dataclass</code>","text":"<p>Bootstrap-based generator that uses your real data as the DGP.</p> <p>Parameters:</p> Name Type Description Default <code>outcomes</code> <code>FloatArray</code> <p>1-D array of observed metric values (e.g. per-user revenue, click indicator, watch-time). For ratio metrics, this argument is used only as a length reference \u2014 the actual per-unit ratio is recomputed each iteration from <code>numerator</code> / <code>denominator</code>. Passing <code>numerator / denominator</code> here works fine; passing zeros of the right length works too.</p> required <code>covariate</code> <code>FloatArray | None</code> <p>Optional 1-D array of pre-experiment covariate values, paired by index with <code>outcomes</code>. Required for CUPED / CUPAC / paired tests.</p> <code>None</code> <code>strata</code> <code>Any | None</code> <p>Optional integer stratum labels (one per row of <code>outcomes</code>). Required for <code>PostStratification</code>. If absent and any criterion needs strata, pass <code>n_strata</code> to bin them post-hoc from the covariate.</p> <code>None</code> <code>numerator</code> <code>FloatArray | None</code> <p>Optional per-unit numerator and denominator arrays for ratio metrics. When both are provided the generator runs in ratio mode: bootstrap-resamples <code>(N_i, D_i)</code> pairs and exposes them as the <code>numerator_*</code> / <code>denominator_*</code> aux arrays consumed by :class:<code>~absim.criteria.DeltaMethod</code> and :class:<code>~absim.criteria.Linearization</code>.</p> <code>None</code> <code>denominator</code> <code>FloatArray | None</code> <p>Optional per-unit numerator and denominator arrays for ratio metrics. When both are provided the generator runs in ratio mode: bootstrap-resamples <code>(N_i, D_i)</code> pairs and exposes them as the <code>numerator_*</code> / <code>denominator_*</code> aux arrays consumed by :class:<code>~absim.criteria.DeltaMethod</code> and :class:<code>~absim.criteria.Linearization</code>.</p> <code>None</code> <code>n_per_group</code> <code>int</code> <p>Sample size per arm in each Monte Carlo iteration. Independent of the size of <code>outcomes</code> \u2014 the bootstrap is with replacement, so you can simulate experiments much larger than your historical data.</p> <code>1000</code> <code>relative</code> <code>bool</code> <p>If <code>True</code>, the simulator's <code>mean_shift</code> is interpreted as a multiplicative lift (<code>rate * (1 + shift)</code>); otherwise as an absolute shift on the metric's natural scale.</p> <code>False</code> <code>n_strata</code> <code>int</code> <p>If <code>strata</code> is not provided but a covariate is, bin the covariate post-hoc into <code>n_strata</code> equal-frequency buckets so stratified criteria still receive sensible labels. Set to <code>1</code> to disable.</p> <code>1</code> <code>name</code> <code>str</code> <p>Free-form label, propagated into reports.</p> <code>'empirical'</code> Behaviour by metric type <p>The generator auto-detects metric type from the data shape:</p> <ol> <li> <p>Ratio \u2014 when both <code>numerator</code> and <code>denominator</code> are provided.    Effect injection is on the numerator: <code>N_t \u2190 N_t * (1 + mean_shift)</code>    in relative mode, or <code>N_t \u2190 N_t + mean_shift * D_t</code> in absolute mode    (so the realised per-unit ratio shifts by exactly <code>mean_shift</code>).</p> </li> <li> <p>Binary \u2014 when <code>outcomes</code> contains only the values <code>{0, 1}</code>.    Effect injection flips a calibrated fraction of zeros to ones (or vice    versa) in the treatment arm so the empirical mean shifts by exactly    <code>mean_shift</code> (absolute) or by <code>baseline * mean_shift</code> (relative).    This preserves the binary structure that the z-test for proportions    expects.</p> </li> <li> <p>Continuous \u2014 anything else. Effect injection is a direct additive    or multiplicative shift on resampled values.</p> </li> </ol>"},{"location":"api/generators/#absim.generators.empirical.EmpiricalGenerator.sample","title":"sample","text":"<pre><code>sample(rng: Generator, mean_shift: float) -&gt; Sample\n</code></pre> <p>Draw one Monte Carlo bootstrap sample with calibrated effect injection.</p>"},{"location":"api/generators/#parametric-generators","title":"Parametric generators","text":""},{"location":"api/generators/#absim.generators.continuous.ContinuousGenerator","title":"absim.generators.continuous.ContinuousGenerator  <code>dataclass</code>","text":"<p>Generator for continuous outcomes with optional pre-experiment covariate.</p> <p>Parameters:</p> Name Type Description Default <code>n_per_group</code> <code>int</code> <p>Sample size per arm.</p> <code>500</code> <code>mean</code> <code>float</code> <p>Baseline mean of the outcome (control).</p> <code>0.0</code> <code>sd</code> <code>float</code> <p>Standard deviation of the outcome's noise term.</p> <code>1.0</code> <code>rho</code> <code>float</code> <p>Correlation between outcome and pre-experiment covariate. Higher <code>|rho|</code> \u21d2 more variance reduction available to CUPED / paired tests.</p> <code>0.5</code> <code>distribution</code> <code>Literal['normal', 'lognormal', 'mixture']</code> <p>Noise model: Gaussian, lognormal, or a heavy-tailed mixture.</p> <code>'normal'</code> <code>n_strata</code> <code>int</code> <p>Number of equal-probability strata indicators emitted as auxiliary data (used by <code>PostStratification</code>). Set to <code>1</code> to disable.</p> <code>4</code> <code>paired</code> <code>bool</code> <p>When <code>True</code>, treatment and control are drawn jointly: pair <code>i</code> shares a latent covariate but has independent noise terms. This is the canonical setup for matched-pair designs (cluster sampling, before/after measurements, ...). When <code>False</code> (default) the two arms are fully independent, matching a standard A/B test. Marginal distributions are identical in both modes; only the within- pair correlation changes. Use <code>paired=True</code> to validate <code>PairedStratification</code>.</p> <code>False</code> <code>name</code> <code>str</code> <p>Free-form label, propagated into reports.</p> <code>'continuous'</code>"},{"location":"api/generators/#absim.generators.continuous.ContinuousGenerator.sample","title":"sample","text":"<pre><code>sample(rng: Generator, mean_shift: float) -&gt; Sample\n</code></pre> <p>Draw one Monte Carlo sample.</p>"},{"location":"api/generators/#absim.generators.binary.BinaryGenerator","title":"absim.generators.binary.BinaryGenerator  <code>dataclass</code>","text":"<p>Generator for binary outcomes (proportions / conversion rates).</p> <p>Parameters:</p> Name Type Description Default <code>n_per_group</code> <code>int</code> <p>Sample size per arm.</p> <code>1000</code> <code>p</code> <code>float</code> <p>Baseline probability under control.</p> <code>0.1</code> <code>rho</code> <code>float</code> <p>Target Pearson correlation between outcome <code>Y</code> and covariate <code>X</code>. Realised within \u2248 1e-3 by numerical calibration of <code>\u03b2</code> (subject to the maximum correlation a logistic link can produce at the requested <code>p</code>; saturates gracefully when asked for the unreachable).</p> <code>0.3</code> <code>n_strata</code> <code>int</code> <p>Number of strata to emit as auxiliary data.</p> <code>4</code> <code>name</code> <code>str</code> <p>Free-form label.</p> <code>'binary'</code>"},{"location":"api/generators/#absim.generators.binary.BinaryGenerator.sample","title":"sample","text":"<pre><code>sample(rng: Generator, mean_shift: float) -&gt; Sample\n</code></pre> <p>Draw one Monte Carlo sample.</p>"},{"location":"api/generators/#absim.generators.ratio.RatioGenerator","title":"absim.generators.ratio.RatioGenerator  <code>dataclass</code>","text":"<p>Generator for ratio metrics with realistic N\u2013D correlation.</p> <p>Parameters:</p> Name Type Description Default <code>n_per_group</code> <code>int</code> <p>Number of users per arm.</p> <code>1000</code> <code>base_rate</code> <code>float</code> <p>The \"true\" per-session ratio under control (e.g. clicks per session).</p> <code>0.2</code> <code>sessions_mean</code> <code>float</code> <p>Mean of the Poisson distribution governing per-user sessions. <code>D_i ~ max(1, Poisson(sessions_mean))</code> to avoid zero denominators.</p> <code>5.0</code> <code>noise</code> <code>float</code> <p>Additional Gaussian noise (in fraction-of-base) on the per-user rate.</p> <code>0.1</code> <code>relative</code> <code>bool</code> <p>If <code>True</code>, <code>mean_shift</code> is interpreted as a multiplicative lift (e.g. <code>0.05</code> \u21d2 +5%); otherwise as an additive shift to the rate.</p> <code>True</code> <code>n_strata</code> <code>int</code> <p>Number of strata buckets emitted as auxiliary data (based on denominator size \u2014 a common choice for ratio metrics).</p> <code>4</code> <code>name</code> <code>str</code> <p>Free-form label.</p> <code>'ratio'</code>"},{"location":"api/generators/#absim.generators.ratio.RatioGenerator.sample","title":"sample","text":"<pre><code>sample(rng: Generator, mean_shift: float) -&gt; Sample\n</code></pre> <p>Draw one Monte Carlo sample.</p>"},{"location":"api/simulator/","title":"<code>absim.simulator</code>","text":""},{"location":"api/simulator/#absim.simulator","title":"absim.simulator","text":"<p>Monte Carlo simulator engine.</p> <p>The simulator drives <code>n_sims</code> independent (data, criterion) iterations. Each iteration draws a fresh :class:<code>~absim.generators.base.Sample</code> and runs <code>criterion.test(...)</code>. The aggregated rejection rate estimates FPR under H\u2080 (<code>effect.value == 0</code>) and Power under H\u2081 otherwise.</p> Reproducibility <p>A single integer <code>seed</code> is split via :class:<code>numpy.random.SeedSequence</code> into <code>n_sims</code> independent child seeds. Each iteration receives one \u2014 so any iteration is bit-identical regardless of parallelism.</p> Parallelism <p>Uses :class:<code>joblib.Parallel</code> with the <code>\"loky\"</code> (process) backend by default. Process-based parallelism sidesteps the GIL and is robust against NumPy/SciPy state. Set <code>parallel=False</code> for serial mode (useful in tests and deterministic benchmarking).</p>"},{"location":"api/simulator/#absim.simulator.Simulator","title":"Simulator  <code>dataclass</code>","text":"<p>Monte Carlo simulator for one (generator, criterion) pair.</p> <p>Parameters:</p> Name Type Description Default <code>generator</code> <code>Generator</code> <p>The synthetic data generator.</p> required <code>criterion</code> <code>Criterion</code> <p>The criterion to evaluate.</p> required <code>n_sims</code> <code>int</code> <p>Number of Monte Carlo iterations.</p> <code>10000</code> <code>alpha</code> <code>float</code> <p>Significance level (used both as the criterion's rejection threshold and as a sanity cross-check in reports).</p> <code>0.05</code> <code>effect</code> <code>EffectSize</code> <p>The effect size to inject. <code>EffectSize(name=\"none\", value=0.0)</code> \u21d2 FPR estimation. Non-zero \u21d2 Power estimation.</p> <code>(lambda: EffectSize(name='none', value=0.0))()</code> <code>seed</code> <code>int</code> <p>Root seed; split deterministically into per-iteration seeds.</p> <code>0</code> <code>n_jobs</code> <code>int</code> <p>Number of parallel workers when <code>parallel=True</code>. <code>-1</code> \u21d2 all cores.</p> <code>-1</code>"},{"location":"api/simulator/#absim.simulator.Simulator.run","title":"run","text":"<pre><code>run(*, parallel: bool = True) -&gt; SimulationReport\n</code></pre> <p>Execute the simulation and return an aggregated :class:<code>SimulationReport</code>.</p>"},{"location":"api/types/","title":"<code>absim.types</code>","text":""},{"location":"api/types/#absim.types","title":"absim.types","text":"<p>Core dataclasses and type aliases used throughout :mod:<code>absim</code>.</p>"},{"location":"api/types/#absim.types.TestResult","title":"TestResult  <code>dataclass</code>","text":"<p>Outcome of running a single statistical criterion on a single sample.</p> <p>Parameters:</p> Name Type Description Default <code>p_value</code> <code>float</code> <p>The two-sided p-value of the test. Must lie in :math:<code>[0, 1]</code>.</p> required <code>statistic</code> <code>float</code> <p>The test statistic (z, t, U, etc.) \u2014 semantics depend on the criterion.</p> required <code>effect</code> <code>float</code> <p>The point estimate of the treatment effect (typically the mean difference, on the metric's natural scale).</p> required <code>std_error</code> <code>float</code> <p>The standard error of the effect estimate, when available. <code>nan</code> if the criterion cannot produce one (e.g. percentile bootstrap).</p> required <code>ci_low</code> <code>float</code> <p>Lower bound of the (1 - alpha) confidence interval for the effect.</p> required <code>ci_high</code> <code>float</code> <p>Upper bound of the (1 - alpha) confidence interval for the effect.</p> required <code>rejected</code> <code>bool</code> <p>Whether the null hypothesis was rejected at the configured <code>alpha</code>.</p> required <code>metadata</code> <code>dict[str, Any]</code> <p>Free-form criterion-specific diagnostics (e.g. CUPED's theta).</p> <code>dict()</code>"},{"location":"api/types/#absim.types.EffectSize","title":"EffectSize  <code>dataclass</code>","text":"<p>A specification of the effect injected into the synthetic data.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>A human-readable label (e.g. <code>\"none\"</code>, <code>\"small\"</code>, <code>\"medium\"</code>).</p> required <code>value</code> <code>float</code> <p>Numeric magnitude on the metric's native scale (mean shift, lift in probability, multiplicative ratio shift, ...). <code>0.0</code> \u21d2 H\u2080.</p> required <code>relative</code> <code>bool</code> <p>Whether <code>value</code> is interpreted as a fractional/multiplicative shift (<code>True</code>) or an absolute shift (<code>False</code>).</p> <code>False</code>"},{"location":"api/types/#absim.types.SimulationReport","title":"SimulationReport  <code>dataclass</code>","text":"<p>Aggregated outcome of <code>N</code> simulations of one (data \u00d7 criterion) pair.</p> <p>The \"rejection rate\" estimates the FPR under H\u2080 (<code>effect.value == 0</code>) and the Power under H\u2081 (<code>effect.value != 0</code>).</p> <p>Attributes:</p> Name Type Description <code>criterion_name</code> <code>str</code> <p>Name of the criterion used.</p> <code>n_sims</code> <code>int</code> <p>Number of Monte Carlo simulations executed.</p> <code>alpha</code> <code>float</code> <p>Nominal significance level used to reject H\u2080.</p> <code>effect</code> <code>EffectSize</code> <p>The injected effect size.</p> <code>rejection_rate</code> <code>float</code> <p>Fraction of simulations rejecting H\u2080. Equals FPR when H\u2080 is true, Power when H\u2081 is true.</p> <code>binomial_ci_low, binomial_ci_high</code> <p>Wilson 95% confidence interval for <code>rejection_rate</code>.</p> <code>mean_pvalue</code> <code>float</code> <p>Mean of all simulation p-values.</p> <code>mean_effect</code> <code>float</code> <p>Mean of all simulation point estimates.</p> <code>mean_std_error</code> <code>float</code> <p>Mean of all simulation standard errors. <code>nan</code> if criterion has none.</p> <code>runtime_sec</code> <code>float</code> <p>Wall-clock time of the simulation, in seconds.</p> <code>metadata</code> <code>dict[str, Any]</code> <p>Free-form simulator-level diagnostics.</p>"},{"location":"api/types/#absim.types.SimulationReport.fpr","title":"fpr  <code>property</code>","text":"<pre><code>fpr: float\n</code></pre> <p>Alias for :attr:<code>rejection_rate</code> when running under H\u2080.</p>"},{"location":"api/types/#absim.types.SimulationReport.power","title":"power  <code>property</code>","text":"<pre><code>power: float\n</code></pre> <p>Alias for :attr:<code>rejection_rate</code> when running under H\u2081.</p>"},{"location":"api/types/#absim.types.SimulationReport.to_dict","title":"to_dict","text":"<pre><code>to_dict() -&gt; dict[str, Any]\n</code></pre> <p>Return a flat dict suitable for tabular reporting / parquet IO.</p>"},{"location":"criteria/","title":"Criterion reference","text":"<p>Each criterion is a small dataclass exposing a single <code>test(treatment, control, **kwargs) -&gt; TestResult</code> method. Pages below give the formula, intuition, assumptions, and references.</p> Page Family Notes Welch's t-test continuous Unequal-variance two-sample t-test. z-test for proportions binary Pooled SE for the test, unpooled for CI. CUPED continuous Variance reduction via pre-experiment cov. CUPAC continuous CUPED with an out-of-fold ML predictor. Bootstrap any Percentile + BCa with jackknife accel. Delta-method ratio Closed-form Taylor variance. Linearization ratio Budylin per-unit linearization. Post-stratification continuous Closed-form weighted mean-difference test. Paired stratification paired Paired t-test on within-pair differences."},{"location":"criteria/bootstrap/","title":"Bootstrap","text":"<p>Non-parametric bootstrap for the difference of means, with percentile and BCa confidence intervals.</p>"},{"location":"criteria/bootstrap/#algorithm","title":"Algorithm","text":"<p>For \\(b = 1, \\ldots, B\\):</p> <ol> <li>Resample treatment and control independently with replacement.</li> <li>Compute \\(\\hat\\theta^*_b = \\bar Y^*_T - \\bar Y^*_C\\).</li> </ol> <p>The resulting bootstrap distribution gives the percentile interval directly. The BCa interval applies bias correction \\(\\hat z_0\\) and skewness correction \\(\\hat a\\) (estimated by jackknife) to the percentile endpoints:</p> \\[ \\hat a = \\frac{\\sum_i (\\bar\\theta_{(\\cdot)} - \\theta_{(i)})^3}               {6 \\left[\\sum_i (\\bar\\theta_{(\\cdot)} - \\theta_{(i)})^2\\right]^{3/2}}, \\] <p>where \\(\\theta_{(i)}\\) are leave-one-out estimates.</p>"},{"location":"criteria/bootstrap/#when-to-prefer-it","title":"When to prefer it","text":"<ul> <li>Heavy-tailed distributions where t-tests overstate confidence.</li> <li>Custom statistics where a closed-form variance is hard to derive   (in this implementation we restrict to the difference of means).</li> </ul>"},{"location":"criteria/bootstrap/#api","title":"API","text":""},{"location":"criteria/bootstrap/#absim.criteria.Bootstrap","title":"absim.criteria.Bootstrap  <code>dataclass</code>","text":"<p>Non-parametric bootstrap for the difference of means.</p> <p>Parameters:</p> Name Type Description Default <code>alpha</code> <code>float</code> <p>Significance level.</p> <code>0.05</code> <code>n_resamples</code> <code>int</code> <p>Number of bootstrap replicates :math:<code>B</code>.</p> <code>2000</code> <code>method</code> <code>Literal['percentile', 'bca']</code> <p><code>\"percentile\"</code> or <code>\"bca\"</code> \u2014 controls which CI / p-value is reported.</p> <code>'percentile'</code> <code>seed</code> <code>int | None</code> <p>Optional integer seed; if <code>None</code> the criterion uses a fresh <code>np.random.default_rng()</code>. The simulator typically passes the per-iteration seed via the <code>rng</code> kwarg instead.</p> <code>None</code>"},{"location":"criteria/bootstrap/#absim.criteria.Bootstrap.test","title":"test","text":"<pre><code>test(\n    treatment: FloatArray,\n    control: FloatArray,\n    **kwargs: Any,\n) -&gt; TestResult\n</code></pre> <p>Run the bootstrap and return percentile or BCa CI + p-value.</p>"},{"location":"criteria/bootstrap/#reference","title":"Reference","text":"<p>Efron, B., &amp; Tibshirani, R. (1994). An Introduction to the Bootstrap. Chapman &amp; Hall.</p>"},{"location":"criteria/cupac/","title":"CUPAC","text":"<p>CUPED with a model-based covariate. Replaces the raw covariate \\(X\\) with the out-of-fold prediction \\(\\hat f(X)\\) of a regression of \\(Y\\) on \\(X\\), fit on the pooled treatment + control sample.</p>"},{"location":"criteria/cupac/#why-pooled-not-per-arm","title":"Why pooled, not per-arm?","text":"<p>Fitting a separate model per arm leaks the treatment effect into the predictor \u2014 under H\u2081 the predictor for the treatment arm absorbs the very shift you're trying to measure, and CUPED's adjustment kills the power. Pooling fits one symmetric regression of \\(Y\\) on \\(X\\), preserves the average treatment effect, and reduces only the within-arm variance.</p>"},{"location":"criteria/cupac/#inputs","title":"Inputs","text":"<p>The criterion accepts either:</p> <ul> <li><code>prediction_treatment</code> + <code>prediction_control</code> \u2014 pre-computed   predictions, the typical production deployment (model trained offline on   pre-experiment data).</li> <li><code>features_treatment</code> + <code>features_control</code> \u2014 feature matrices, in   which case <code>absim</code> fits a <code>RidgeCV</code> with K-fold OOF predictions.</li> </ul>"},{"location":"criteria/cupac/#api","title":"API","text":""},{"location":"criteria/cupac/#absim.criteria.CUPAC","title":"absim.criteria.CUPAC  <code>dataclass</code>","text":"<p>CUPAC: CUPED with an out-of-fold ML prediction as the covariate.</p> <p>Parameters:</p> Name Type Description Default <code>alpha</code> <code>float</code> <p>Significance level.</p> <code>0.05</code> <code>n_splits</code> <code>int</code> <p>Number of CV folds when fitting the default model in-place. Ignored when callers pass <code>prediction_*</code> directly.</p> <code>5</code> <code>seed</code> <code>int</code> <p>Random state for the KFold splitter when fitting in-place.</p> <code>0</code>"},{"location":"criteria/cupac/#absim.criteria.CUPAC.test","title":"test","text":"<pre><code>test(\n    treatment: FloatArray,\n    control: FloatArray,\n    **kwargs: Any,\n) -&gt; TestResult\n</code></pre> <p>Run a CUPAC-adjusted Welch t-test.</p>"},{"location":"criteria/cuped/","title":"CUPED","text":"<p>Controlled-experiment Using Pre-Experiment Data. Reduces variance by removing the linear component of a pre-experiment covariate \\(X\\) from the outcome \\(Y\\).</p>"},{"location":"criteria/cuped/#definition","title":"Definition","text":"\\[ \\theta = \\frac{\\operatorname{Cov}(Y, X)}{\\operatorname{Var}(X)}, \\qquad Y_{\\mathrm{adj}} = Y - \\theta\\, (X - \\mathbb{E} X), \\] <p>then run Welch's t-test on \\(Y_{\\mathrm{adj}}\\). Asymptotic variance reduction is</p> \\[ \\frac{\\operatorname{Var}(Y_{\\mathrm{adj}})}{\\operatorname{Var}(Y)} = 1 - \\rho^2, \\] <p>where \\(\\rho = \\operatorname{Corr}(Y, X)\\).</p>"},{"location":"criteria/cuped/#assumptions","title":"Assumptions","text":"<ul> <li>\\(X\\) is independent of treatment assignment (drawn before the experiment).</li> <li>The relationship \\(Y \\mid X\\) is well-approximated by a single linear slope   on the pooled sample.</li> </ul>"},{"location":"criteria/cuped/#api","title":"API","text":""},{"location":"criteria/cuped/#absim.criteria.CUPED","title":"absim.criteria.CUPED  <code>dataclass</code>","text":"<p>CUPED-adjusted Welch t-test.</p> <p>The covariate must be supplied per-call via the <code>covariate_treatment</code> and <code>covariate_control</code> keyword arguments (typically a per-unit pre-experiment metric).</p> <p>Parameters:</p> Name Type Description Default <code>alpha</code> <code>float</code> <p>Significance level. Used for the CI and the rejection decision.</p> <code>0.05</code>"},{"location":"criteria/cuped/#absim.criteria.CUPED.test","title":"test","text":"<pre><code>test(\n    treatment: FloatArray,\n    control: FloatArray,\n    **kwargs: Any,\n) -&gt; TestResult\n</code></pre> <p>Run a CUPED-adjusted Welch t-test.</p>"},{"location":"criteria/cuped/#reference","title":"Reference","text":"<p>Deng, A., Xu, Y., Kohavi, R., &amp; Walker, T. (2013). Improving the sensitivity of online controlled experiments by utilizing pre-experiment data. WSDM '13.</p>"},{"location":"criteria/delta_method/","title":"Delta-method","text":"<p>For a ratio metric \\(\\theta = \\bar N / \\bar D\\) computed over per-unit \\((N_i, D_i)\\) pairs, the Taylor expansion gives</p> \\[ \\widehat{\\operatorname{Var}}(\\hat\\theta) \\approx     \\frac{1}{\\bar D^2} \\widehat{\\operatorname{Var}}(\\bar N)     - \\frac{2 \\bar N}{\\bar D^3} \\widehat{\\operatorname{Cov}}(\\bar N, \\bar D)     + \\frac{\\bar N^2}{\\bar D^4} \\widehat{\\operatorname{Var}}(\\bar D), \\] <p>and the test statistic for \\(\\hat\\theta_T - \\hat\\theta_C\\) is the difference divided by \\(\\sqrt{\\widehat{\\operatorname{Var}}_T + \\widehat{\\operatorname{Var}}_C}\\), referred to a standard normal.</p>"},{"location":"criteria/delta_method/#when-to-prefer-it","title":"When to prefer it","text":"<ul> <li>Ratio metrics where the numerator and denominator are observed at   different granularities (clicks per session, purchases per visit).</li> <li>When you want a closed-form CI/p-value without resampling.</li> </ul>"},{"location":"criteria/delta_method/#api","title":"API","text":""},{"location":"criteria/delta_method/#absim.criteria.DeltaMethod","title":"absim.criteria.DeltaMethod  <code>dataclass</code>","text":"<p>Delta-method z-test for the difference of two ratio metrics.</p> Required kwargs <p><code>numerator_treatment</code>, <code>denominator_treatment</code> and the corresponding <code>*_control</code> per-unit arrays (each of length <code>n_t</code> / <code>n_c</code>). The convention is to ignore the <code>treatment</code> and <code>control</code> positional args (those are kept for protocol compatibility \u2014 they're typically the realised per-unit ratio :math:<code>N_i / D_i</code>).</p>"},{"location":"criteria/delta_method/#absim.criteria.DeltaMethod.test","title":"test","text":"<pre><code>test(\n    treatment: FloatArray,\n    control: FloatArray,\n    **kwargs: Any,\n) -&gt; TestResult\n</code></pre> <p>Run a delta-method z-test on a difference of ratios.</p>"},{"location":"criteria/delta_method/#reference","title":"Reference","text":"<p>Deng, A., Knoblich, U., &amp; Lu, J. (2018). Applying the Delta Method in Metric Analytics: A Practical Guide with Novel Ideas. KDD.</p>"},{"location":"criteria/linearization/","title":"Linearization (Budylin)","text":"<p>Reduce the ratio \\(\\bar N / \\bar D\\) to an additive per-unit metric</p> \\[ L_i = \\frac{N_i}{\\bar D} - \\frac{\\bar N}{\\bar D^2} D_i, \\] <p>then run any sample-mean test on \\(L\\) (here Welch's t-test). To leading order this is identical to the delta method, but lets you drop in further variance reduction (CUPED, post-stratification) on top of \\(L\\) \u2014 they all just operate on a per-unit metric.</p>"},{"location":"criteria/linearization/#implementation-note","title":"Implementation note","text":"<p><code>absim</code> linearizes against the pooled numerator and denominator means (Budylin's recipe), so both arms share the same linear functional.</p>"},{"location":"criteria/linearization/#api","title":"API","text":""},{"location":"criteria/linearization/#absim.criteria.Linearization","title":"absim.criteria.Linearization  <code>dataclass</code>","text":"<p>Budylin linearization + Welch's t-test on the linearized metric.</p> Required kwargs <p><code>numerator_treatment</code>, <code>denominator_treatment</code>, <code>numerator_control</code>, <code>denominator_control</code>.</p>"},{"location":"criteria/linearization/#absim.criteria.Linearization.test","title":"test","text":"<pre><code>test(\n    treatment: FloatArray,\n    control: FloatArray,\n    **kwargs: Any,\n) -&gt; TestResult\n</code></pre> <p>Run a Welch t-test on the Budylin-linearized ratio metric.</p>"},{"location":"criteria/linearization/#reference","title":"Reference","text":"<p>Budylin, R. (2018). Consistent transformation of ratio metrics for efficient online controlled experiments.</p>"},{"location":"criteria/paired_stratification/","title":"Paired stratification","text":"<p>Paired t-test on within-pair differences \\(d_i = T_i - C_i\\).</p>"},{"location":"criteria/paired_stratification/#when-to-use-it","title":"When to use it","text":"<p><code>PairedStratification(paired=True)</code> (default) requires genuinely paired data \u2014 for example before/after measurements, twin studies, or the output of a paired generator. In that case the differences are i.i.d. and the classical paired-t standard error is correct.</p> <p>For independent A/B samples, paired matching by covariate rank (<code>paired=False</code>) is approximate \u2014 see the architecture notes for the small FPR inflation it introduces. We recommend using CUPED or <code>PostStratification</code> for unpaired data, and reserving the paired test for the cases where pairing is real.</p>"},{"location":"criteria/paired_stratification/#api","title":"API","text":""},{"location":"criteria/paired_stratification/#absim.criteria.PairedStratification","title":"absim.criteria.PairedStratification  <code>dataclass</code>","text":"<p>Matched-pairs t-test on within-pair differences.</p> <p>Parameters:</p> Name Type Description Default <code>alpha</code> <code>float</code> <p>Significance level.</p> <code>0.05</code> <code>paired</code> <code>bool</code> <p>If <code>True</code> (default), <code>treatment[i]</code> and <code>control[i]</code> are assumed to be a real pair (e.g. from a paired generator) \u2014 the test uses <code>d_i = T_i - C_i</code> directly. If <code>False</code>, units are paired post-hoc by sorting both arms by the supplied covariate rank.</p> <code>True</code>"},{"location":"criteria/paired_stratification/#absim.criteria.PairedStratification.test","title":"test","text":"<pre><code>test(\n    treatment: FloatArray,\n    control: FloatArray,\n    **kwargs: Any,\n) -&gt; TestResult\n</code></pre> <p>Run a paired t-test on within-pair differences.</p>"},{"location":"criteria/post_stratification/","title":"Post-stratification","text":"<p>Closed-form stratified estimator. Given strata indicators \\(s_i \\in \\{1, \\ldots, K\\}\\),</p> \\[ \\hat\\Delta = \\sum_{k=1}^K w_k\\, (\\bar Y^T_k - \\bar Y^C_k), \\qquad w_k = \\frac{n_k}{n} \\] <p>with the pooled-sample weights \\(w_k\\). Closed-form variance:</p> \\[ \\widehat{\\operatorname{Var}}(\\hat\\Delta) = \\sum_k w_k^2     \\left(\\frac{s^2_{T,k}}{n_{T,k}} + \\frac{s^2_{C,k}}{n_{C,k}}\\right). \\] <p>The resulting test is referred to a Welch\u2013Satterthwaite t-distribution.</p>"},{"location":"criteria/post_stratification/#why-pooled-weights","title":"Why pooled weights?","text":"<p>If stratum sizes are estimated per arm, the difference contains a random-weight term that increases variance. Using pooled weights \u2014 which are independent of treatment assignment under randomisation \u2014 removes that term. This is the Miratrix\u2013Sekhon\u2013Yu result; under stratified randomisation it is guaranteed not to hurt vs. the unstratified Welch test.</p>"},{"location":"criteria/post_stratification/#api","title":"API","text":""},{"location":"criteria/post_stratification/#absim.criteria.PostStratification","title":"absim.criteria.PostStratification  <code>dataclass</code>","text":"<p>Post-stratification estimator with closed-form variance.</p> <p>Parameters:</p> Name Type Description Default <code>alpha</code> <code>float</code> <p>Significance level.</p> <code>0.05</code>"},{"location":"criteria/post_stratification/#absim.criteria.PostStratification.test","title":"test","text":"<pre><code>test(\n    treatment: FloatArray,\n    control: FloatArray,\n    **kwargs: Any,\n) -&gt; TestResult\n</code></pre> <p>Run a post-stratified mean-difference test.</p>"},{"location":"criteria/welch/","title":"Welch's t-test","text":"<p>Two-sample t-test for the difference of means under unequal variances.</p>"},{"location":"criteria/welch/#statistic","title":"Statistic","text":"\\[ t = \\frac{\\bar Y_T - \\bar Y_C}{\\sqrt{s_T^2/n_T + s_C^2/n_C}}, \\qquad \\nu = \\frac{(s_T^2/n_T + s_C^2/n_C)^2}{(s_T^2/n_T)^2/(n_T-1) + (s_C^2/n_C)^2/(n_C-1)}. \\]"},{"location":"criteria/welch/#assumptions","title":"Assumptions","text":"<ul> <li>Independence within and between arms.</li> <li>Means are well-defined (i.e. finite-variance outcomes).</li> <li>Sample sizes are large enough for the t-distribution to be a good   approximation (<code>n_per_group \u2273 30</code> is plenty for not-too-skewed data).</li> </ul>"},{"location":"criteria/welch/#when-to-prefer-it","title":"When to prefer it","text":"<ul> <li>Default for continuous metrics.</li> <li>Use as the comparison floor for variance-reduction methods (CUPED, post-strat).</li> </ul>"},{"location":"criteria/welch/#api","title":"API","text":""},{"location":"criteria/welch/#absim.criteria.WelchTTest","title":"absim.criteria.WelchTTest  <code>dataclass</code>","text":"<p>Welch's t-test for the difference of two means with unequal variances.</p> <p>Parameters:</p> Name Type Description Default <code>alpha</code> <code>float</code> <p>Significance level used to construct the CI and decide rejection.</p> <code>0.05</code> Notes <p>The test statistic is</p> <p>.. math::     t = \\frac{\\bar{Y}_T - \\bar{Y}_C}               {\\sqrt{s_T^2/n_T + s_C^2/n_C}}</p> <p>with Welch\u2013Satterthwaite degrees of freedom</p> <p>.. math::     \\nu = \\frac{(s_T^2/n_T + s_C^2/n_C)^2}                  {(s_T^2/n_T)^2/(n_T-1) + (s_C^2/n_C)^2/(n_C-1)}.</p>"},{"location":"criteria/welch/#absim.criteria.WelchTTest.test","title":"test","text":"<pre><code>test(\n    treatment: FloatArray,\n    control: FloatArray,\n    **kwargs: Any,\n) -&gt; TestResult\n</code></pre> <p>Run Welch's t-test and return a :class:<code>TestResult</code>.</p>"},{"location":"criteria/welch/#reference","title":"Reference","text":"<p>Welch, B. L. (1947). The generalization of \"Student's\" problem when several different population variances are involved. Biometrika, 34(1/2), 28\u201335.</p>"},{"location":"criteria/z_proportion/","title":"z-test for proportions","text":"<p>Two-sample z-test for the difference between Bernoulli probabilities.</p>"},{"location":"criteria/z_proportion/#statistic","title":"Statistic","text":"\\[ z = \\frac{\\hat p_T - \\hat p_C}{\\sqrt{\\bar p (1 - \\bar p)(1/n_T + 1/n_C)}}, \\] <p>where \\(\\bar p\\) is the pooled proportion under H\u2080. The CI uses an unpooled SE: \\(\\sqrt{\\hat p_T (1 - \\hat p_T)/n_T + \\hat p_C (1 - \\hat p_C)/n_C}\\).</p>"},{"location":"criteria/z_proportion/#assumptions","title":"Assumptions","text":"<ul> <li>Independent Bernoulli observations within each arm.</li> <li>\\(n_T \\hat p_T (1 - \\hat p_T)\\) and the equivalent for control are large   enough for the normal approximation (rule of thumb \u2265 5 successes and \u2265 5   failures per arm).</li> </ul>"},{"location":"criteria/z_proportion/#api","title":"API","text":""},{"location":"criteria/z_proportion/#absim.criteria.ZTestProportions","title":"absim.criteria.ZTestProportions  <code>dataclass</code>","text":"<p>Two-sample z-test for proportions.</p> <p>Parameters:</p> Name Type Description Default <code>alpha</code> <code>float</code> <p>Significance level. Used for the unpooled-SE CI and rejection rule.</p> <code>0.05</code>"},{"location":"criteria/z_proportion/#absim.criteria.ZTestProportions.test","title":"test","text":"<pre><code>test(\n    treatment: FloatArray,\n    control: FloatArray,\n    **kwargs: Any,\n) -&gt; TestResult\n</code></pre> <p>Run a two-sample z-test on 0/1 data.</p>"}]}